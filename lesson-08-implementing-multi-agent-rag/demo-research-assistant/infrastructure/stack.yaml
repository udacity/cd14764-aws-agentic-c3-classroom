AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Lesson 8 Demo: Multi-Agent RAG for Research Assistant
  Creates the S3 source bucket that holds documents for the CS Papers and
  Biology Papers Knowledge Bases.
  NOTE: Bedrock Knowledge Bases must be created manually in the AWS Console.

Parameters:
  ProjectName:
    Type: String
    Default: lesson-08-demo-rag

Resources:
  KBSourceBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub ${ProjectName}-kb-sources-${AWS::AccountId}
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

Outputs:
  KBSourceBucketName:
    Value: !Ref KBSourceBucket
    Export:
      Name: !Sub ${ProjectName}-KBSourceBucket
