# ─────────────────────────────────────────────────────────────────────────────
# Lesson 8 Exercise: Multi-Agent RAG for Clinical Literature
#
# Copy this file to .env and fill in your values.
# The .env file is gitignored — never commit real credentials.
#
# Bedrock Knowledge Bases require us-west-2 (Oregon) or us-east-1 (N. Virginia).
#
# Setup steps:
#   1. Deploy S3 source bucket:
#        aws cloudformation deploy --template-file infrastructure/stack.yaml \
#            --stack-name lesson-08-exercise-rag
#   2. Upload documents to S3:
#        python seed_documents.py
#   3. Create the Drug Interactions and Clinical Guidelines Knowledge Bases in
#      the Bedrock console. For each KB:
#        - Data source: S3 → point at s3://<bucket>/drugs/ (or /guidelines/)
#        - Embedding model: amazon.titan-embed-text-v2:0
#        - Vector store: Amazon S3 Vectors
#        - Click Sync after creation so the documents are ingested
#   4. Copy the two KB IDs into the variables below.
# ─────────────────────────────────────────────────────────────────────────────

# ── AWS / Bedrock Settings ───────────────────────────────────────────────────
AWS_REGION=us-west-2
NOVA_LITE_MODEL=us.amazon.nova-lite-v1:0
NOVA_PRO_MODEL=us.amazon.nova-pro-v1:0

# ── Exercise Knowledge Base IDs (create manually in AWS Console) ─────────────
DRUG_INTERACTIONS_KB_ID=
CLINICAL_GUIDELINES_KB_ID=
